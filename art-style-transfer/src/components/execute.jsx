// import { exec } from 'child_process';

// const runPythonScripts = () => {
//     exec('python frame.py', (error, stdout, stderr) => {
//         if (error) {
//             console.error(`Error executing frame.py: ${error}`);
//             return;
//         }
//         console.log(`frame.py output: ${stdout}`);
//         console.error(`frame.py errors: ${stderr}`);
//     });

//     exec('python img_color.py', (error, stdout, stderr) => {
//         if (error) {
//             console.error(`Error executing img_color.py: ${error}`);
//             return;
//         }
//         console.log(`img_color.py output: ${stdout}`);
//         console.error(`img_color.py errors: ${stderr}`);
//     });

//     exec('python reframe.py', (error, stdout, stderr) => {
//         if (error) {
//             console.error(`Error executing reframe.py: ${error}`);
//             return;
//         }
//         console.log(`reframe.py output: ${stdout}`);
//         console.error(`reframe.py errors: ${stderr}`);
//     });

//     exec('python delf.py', (error, stdout, stderr) => {
//         if (error) {
//             console.error(`Error executing delf.py: ${error}`);
//             return;
//         }
//         console.log(`delf.py output: ${stdout}`);
//         console.error(`delf.py errors: ${stderr}`);
//     });
// }

// export default runPythonScripts;
